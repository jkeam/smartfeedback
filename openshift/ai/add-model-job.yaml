apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: add-model-job-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi

---

apiVersion: batch/v1
kind: Job
metadata:
  name: add-model-job
spec:
  backoffLimit: 4
  template:
    spec:
      serviceAccount: feedback-ai-sa
      serviceAccountName: feedback-ai-sa
      initContainers:
      containers:
      - name: add-model
        image: "quay.io/jkeam/granite-7b@sha256:c090218bd895bdcf859265e0e5bc9dac205b7ff24ec87e336aec450de7c95db0"
        imagePullPolicy: IfNotPresent
        envFrom:
        - secretRef:
            name: aws-connection-minio
        volumeMounts:
            - name: temp
              mountPath: /tmp/models
      restartPolicy: Never
      volumes:
        - name: temp
          persistentVolumeClaim:
            claimName: add-model-job-pvc
