apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: add-model-job-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi

---

apiVersion: batch/v1
kind: Job
metadata:
  name: add-model-job
spec:
  backoffLimit: 4
  template:
    spec:
      serviceAccount: feedback-ai-sa
      serviceAccountName: feedback-ai-sa
      initContainers:
      containers:
      - name: add-model
        image: "quay.io/jkeam/granite-7b@sha256:65c085b167def5e8f8931836435af09c454ff1114a688085ca067e9c5aef7b3f"
        imagePullPolicy: IfNotPresent
        envFrom:
        - secretRef:
            name: aws-connection-minio
        volumeMounts:
        - name: models
          mountPath: /opt/app-root/models
        resources:
          requests:
            memory: "16Gi"
            cpu: "500m"
            ephemeral-storage: "16Gi"
          limits:
            memory: "24Gi"
            cpu: "2000m"
            ephemeral-storage: "24Gi"
      restartPolicy: Never
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: add-model-job-pvc
